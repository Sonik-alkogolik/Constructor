     1. Цель конструктора:
        - Визуальное редактирование вставленного HTML/CSS? у нас будет в вкладки с выпадающим input или как лучше 
        это будет как tab прик клике на tab будет выпадать блок можно вставить html воторой tab для css и скрвывать при клике на крестик закрывается блок куда можно вставлять что бы он нам не мешал
        - Предпросмотр с возможностью правки кода? Предпросмотр в другой вкладке

     2. Какой функционал нужен в первую очередь:
        - 🔲 Выделение элементов кликом (допустим у нас разметка <nav  class="navbar"><div  class="navbar-container"></div></nav>)  появляются вверху этого элемента его свойства max-width width height по типу с флажками поверх элемента и это всё со стелками по примеру range или + - 
        - 📐 Изменение размеров, отступов, позиционирования?по типу с флажками поверх элемента путм добавления + - я могу пенять любое своейство 
        - 🎨 Редактор CSS свойств (цвета, шрифты, размеры)?по типу с флажками поверх элемента путм добавления + - я могу пенять любое своейство 
        - 📱 Предпросмотр на разных брейкпоинтах? (нужно добавить каждый brakpoint на html странице каждый в отедльно котейнере в котором шириа контенера будет отвеать ширине и выот брэкпоинта)
        - если есть варинат то в спомощью вычислений подгонять самостоятельно контент контейнера под ширину родителя 
        пример допустим у нас есть конейнер div  c классом .wrapp и его max-width:1200px: width:100% а в нём допустим div .wrapp__item и когда брекпоинт max-wdth:1080px   ширина эркана 1080 то  .wrapp будет max-width:1080px а ширина div .wrapp__item будет всегда на 1px и слева и спрва меньше нужно что бы немного отсупт но было по всей ширине /
        - так же продумать если текст или карточка товара в пкшв прилепает то текст переносился интуитивная адаптивная вёрстка но так же при клике на текст так же влажками открывались вверху интерфейс что бы сделать меньше font-size line height и т.д
         нужно все доуступные настройки судя из css выводить 
         - 📤 Экспорт результата (HTML/CSS файл, код в буфер)? да по клику на кнопку отдельно html отдельно css две разные кнопки 
     .  3 Сохранение проекта:
        - Локально в браузере (localStorage)?
        - Скачивание файлов?
        - Без сохранения (только сессия)?

попробуй вставить в html  
html
 <div class="wrapp">
     <div class="wrapp_inner">

     </div>
 </div> 
   

css
 .wrapp {
   max-wridth:1200px;
 }
    .wrapp_inner {
        max-width:100%;
        width:100%;
    }
